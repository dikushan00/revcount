

export const config = {
    layoutIds: {
        allWidth: 2, //full width layout
        container: 3, //layout with container
        main: 1, //main layout with sidebars
        containerWNav: 4 //container layout with sidebars on left side
    }
}